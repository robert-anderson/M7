//
// Created by Robert John Anderson on 2020-01-31.
//

#include <gtest/gtest.h>

/*
#include "../../src/enumerator/SetBitEnumerator.h"

TEST(SetBitEnumerator, Bitfield) {
    Bitfield bitfield(200);
    defs::inds setbits{1, 4, 7, 32, 89, 123, 199};
    bitfield.set(setbits);
    SetBitEnumerator enumerator(bitfield);

    size_t setbit; while(enumerator.next(setbit)){
        ASSERT_TRUE(setbits[enumerator.counter()]==setbit);
    }
}
*/